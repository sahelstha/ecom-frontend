import { paginationClasses } from "@mui/material";

const initialState = {
  adminOrder: null,
  pagination: {},
};

export const orderReducer = (state = initialState, action) => {
  return state;
};
